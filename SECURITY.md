# Security policy

Do not report vulnerabilities through public issue content if they include secrets, private project artifacts, or a viable exploit. Instead, contact the repository maintainer privately with a concise description, affected version, and safe reproduction steps.

This prototype performs local advisory checks only. It should not be exposed to an untrusted network without authentication, authorization, HTTPS, secret management, and an independent security review.
