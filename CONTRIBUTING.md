# Contributing

Keep changes local-first, typed, and tested. Do not add claims of safety certification or high-fidelity simulation without evidence. Keep parser and telemetry input handling non-executing and bounded.

Before opening a pull request, run the unit tests and manually try the core path: dashboard → simulate → fault → test evidence → compare → report. Do not commit secrets, generated databases, or reports.
